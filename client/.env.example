# Leave empty in web mode (Vite proxy handles /api/* → localhost:3001)
# Set to your backend URL when building the native Capacitor app:
#   - Dev (local network):  http:// 192.168.0.120:3001
#   - Production (Railway): https://your-backend.railway.app
VITE_API_BASE_URL=
